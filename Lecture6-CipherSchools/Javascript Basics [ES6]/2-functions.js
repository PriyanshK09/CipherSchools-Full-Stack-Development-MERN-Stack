var add = function (x, y) {
  return x + y;
};

const addArrow = (x, y) => {
  return x + y;
};

console.log(addArrow(5, 6));

const addArrowCompact = (x, y) => x + y;

console.log(addArrowCompact(5, 6));
